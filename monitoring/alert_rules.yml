# DO NOT EDIT ? GENERATED: Prometheus Alerts (monitoring/alert_rules.yml)
# Sources:
#  - monitoring/alert_rules.d/a1-api-latency__sample.yml
groups:
  - name: a1-api
    rules:
      - alert: A1ApiHighLatency
        annotations:
          id: A1-alerts-api-latency
          summary: A1 API high latency
        expr: histogram_quantile(0.95, sum(rate(http_request_duration_seconds_bucket{service="a1"}[5m])) by (le))
        for: 10m
        labels:
          severity: warning
