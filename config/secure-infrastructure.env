# ===========================================
# 安全基础设施连接配置 (轮换后)
# ===========================================
# 工程师C - 安全凭证轮换后配置
# 所有敏感凭证已轮换，使用新的安全密钥

# ===========================================
# 基础环境配置
# ===========================================
NODE_ENV=production
PORT=8001

# ===========================================
# 轮换后Redis配置
# ===========================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=athlete_ally_redis_2024_secure_v2_1726123456
REDIS_DB=0
REDIS_URL=redis://:athlete_ally_redis_2024_secure_v2_1726123456@localhost:6379/0

# Redis连接池配置
REDIS_MAX_CONNECTIONS=20
REDIS_MIN_CONNECTIONS=5
REDIS_CONNECTION_TIMEOUT=10000
REDIS_COMMAND_TIMEOUT=5000

# ===========================================
# 轮换后PostgreSQL配置
# ===========================================
DATABASE_URL=postgresql://athlete_ally_user:athlete_ally_db_2024_secure_v2_1726123456@localhost:5432/athlete_ally_main
DB_HOST=localhost
DB_PORT=5432
DB_USER=athlete_ally_user
DB_PASSWORD=athlete_ally_db_2024_secure_v2_1726123456
DB_NAME=athlete_ally_main

# 数据库连接池配置
DB_MAX_CONNECTIONS=20
DB_MIN_CONNECTIONS=5
DB_CONNECTION_TIMEOUT=10000
DB_QUERY_TIMEOUT=30000

# ===========================================
# 协议引擎专用配置
# ===========================================
PROTOCOL_DATABASE_URL=postgresql://athlete_ally_user:athlete_ally_db_2024_secure_v2_1726123456@localhost:5432/athlete_ally_main
PROTOCOL_REDIS_URL=redis://:athlete_ally_redis_2024_secure_v2_1726123456@localhost:6379/1

# ===========================================
# 缓存配置
# ===========================================
CACHE_PROTOCOL_SUMMARY_TTL=3600
CACHE_PROTOCOL_DETAILS_TTL=1800
CACHE_PROTOCOL_PERMISSIONS_TTL=900
CACHE_USER_PROTOCOLS_TTL=1800
CACHE_PUBLIC_PROTOCOLS_TTL=3600

# 缓存键前缀
CACHE_KEY_PREFIX=athlete_ally:protocol_engine:

# ===========================================
# 性能优化配置
# ===========================================
QUERY_OPTIMIZATION_ENABLED=true
QUERY_CACHE_ENABLED=true
QUERY_CACHE_TTL=300

# 批量操作配置
BATCH_OPERATION_SIZE=100
BATCH_OPERATION_TIMEOUT=5000

# ===========================================
# 监控和日志配置
# ===========================================
PERFORMANCE_MONITORING_ENABLED=true
QUERY_METRICS_ENABLED=true
CACHE_METRICS_ENABLED=true

# 日志级别
LOG_LEVEL=info
LOG_FORMAT=json

# ===========================================
# 轮换后安全配置
# ===========================================
# JWT配置 (已轮换)
JWT_SECRET=athlete_ally_jwt_2024_secure_key_v2_1726123456
JWT_EXPIRES_IN=24h

# 加密配置 (已轮换)
ENCRYPTION_KEY=athlete_ally_encryption_2024_secure_v2_1726123456
ENCRYPTION_ALGORITHM=aes-256-gcm

# ===========================================
# 服务发现配置
# ===========================================
PROTOCOL_ENGINE_PORT=8001
CACHE_SERVICE_PORT=8002
OPTIMIZATION_SERVICE_PORT=8003

# 健康检查
HEALTH_CHECK_ENABLED=true
HEALTH_CHECK_INTERVAL=30000

# ===========================================
# 测试环境配置
# ===========================================
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/athlete_ally_test
TEST_REDIS_URL=redis://localhost:6379/1

# 测试配置
TEST_MODE=false
TEST_DATA_CLEANUP=true
