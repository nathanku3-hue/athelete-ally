{
  "$schema": "https://json-schema.org/draft/2019-09/schema",
  "$id": "https://athlete-ally.dev/schemas/normalized/heart_rate.v1.json",
  "title": "Normalized Heart Rate v1",
  "type": "object",
  "$defs": {
    "metadata": {
      "type": "object",
      "properties": {
        "version": { "type": "string", "const": "v1" },
        "domain": { "type": "string" },
        "user_id": { "type": "string", "minLength": 1 },
        "received_at": { "type": "string", "format": "date-time" },
        "source": {
          "type": "object",
          "properties": {
            "provider": { "type": "string" },
            "event_id": { "type": "string" },
            "delivery_id": { "type": "string" }
          },
          "required": ["provider"],
          "additionalProperties": true
        }
      },
      "required": ["version", "domain", "user_id", "received_at", "source"],
      "additionalProperties": false
    }
  },
  "properties": {
    "version": { "type": "string", "const": "v1" },
    "domain": { "type": "string", "const": "heart_rate" },
    "user_id": { "type": "string" },
    "received_at": { "type": "string", "format": "date-time" },
    "source": { "\": "#//metadata/properties/source" },
    "samples": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "timestamp": { "type": "string", "format": "date-time" },
          "bpm": { "type": "number", "minimum": 0 }
        },
        "required": ["timestamp", "bpm"],
        "additionalProperties": false
      }
    },
    "summary": {
      "type": "object",
      "properties": {
        "min": { "type": "number", "minimum": 0 },
        "max": { "type": "number", "minimum": 0 },
        "avg": { "type": "number", "minimum": 0 }
      },
      "additionalProperties": false
    }
  },
  "required": ["version", "domain", "user_id", "received_at", "source", "samples"],
  "additionalProperties": false
}

